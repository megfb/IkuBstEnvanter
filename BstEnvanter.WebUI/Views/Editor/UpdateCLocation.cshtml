@model BstEnvanter.WebUI.Models.UpdateCLocationViewModel
@{
    ViewData["Title"] = "UpdateCLocation";
    Layout = "~/Views/_Layout.cshtml";
}
<div class="container-fluid">
        <div class="jumbotron jumbotron-fluid">
        <h1 class="display-4">Update a Location</h1>
        <hr />
    </div>
    <form asp-action="updateclocation" class="col-md-8" method="post">
        <div class="form-group">
            <input asp-for="cLocation.name" class="form-control" type="text" placeholder="@Model.cLocation.name" />
            <span asp-validation-for="cLocation.name"></span>
        </div>
        <div class="form-group text-end">
            <input type="hidden" asp-for="cLocation.id" />
            <input type="submit" value="Update" class="btn btn-success col-md-2">
            <a class="btn btn-info col-md-2" asp-action="listofclocation">Back</a>
        </div>
    </form>
</div>