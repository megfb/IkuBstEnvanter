@model BstEnvanter.WebUI.Models.AddGpuViewModel
@{
    ViewData["Title"] = "AddGpu";
    Layout = "~/Views/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="jumbotron jumbotron-fluid">
        <h1 class="display-4">Add a New Gpu</h1>
        <hr />
    </div>
    <form asp-action="addgpu" class="col-md-8" method="post">
        <div class="form-group">
            <input asp-for="gpu.name" class="form-control" type="text" placeholder="Name" />
            <span asp-validation-for="gpu.name"></span>
        </div>
        <div class="mt-1 text-end">
            <button class="btn btn-primary col-md-2">Add</button>
            <a asp-action="listofgpu" class="btn btn-info col-md-2">Back</a>
        </div>
    </form>
</div>
