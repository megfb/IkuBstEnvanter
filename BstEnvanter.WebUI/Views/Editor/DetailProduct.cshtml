@model BstEnvanter.WebUI.Models.UpdateProductViewModel
@{
    ViewData["Title"] = "DetailProduct";
    Layout = "~/Views/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="jumbotron jumbotron-fluid">
        <h1 class="display-4">Ürün Detayları</h1>
        <hr />
    </div>
    <div class="row">
        <div class="row col-md-12">
            @if (Model.status.personelId == null && Model.status.commonId == null)
            {
                <div class=" border col-md-6 rounded">

                    <div class="row bg-primary">
                        <h3 class="pt-3 text-light">Ürün Detayları</h3>
                    </div>
                    <table class="table table-borderless">
                        <tbody>

                            <tr>
                                <td>Ad:</td>
                                <td>@Model.status.Product.name</td>
                            </tr>
                            <tr>
                                <td>Kategori:</td>
                                @if (Model.status.Product.categoryId != null)
                                {
                                    <td>@Model.status.Product.Category.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Marka:</td>
                                @if (Model.status.Product.brandId != null)
                                {
                                    <td>@Model.status.Product.Brand.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Model:</td>
                                @if (Model.status.Product.modelId != null)
                                {
                                    <td>@Model.status.Product.Model.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Seri Numarası:</td>
                                <td>@Model.status.Product.serialNo</td>
                            </tr>
                            <tr>
                                <td>Envanter Numarası:</td>
                                <td>@Model.status.Product.inventoryNo</td>
                            </tr>

                            <tr>
                                <td>İşlemci:</td>

                                @if (Model.status.Product.cpuId != null)
                                {
                                    <td>@Model.status.Product.Cpu.name</td>
                                }

                            </tr>
                            <tr>
                                <td>Ekran Kartı:</td>
                                @if (Model.status.Product.gpuId != null)
                                {
                                    <td>@Model.status.Product.Gpu.name</td>
                                }
                            </tr>
                            <tr>

                                <td>Ram:</td>
                                @if (Model.status.Product.ramId != null)
                                {
                                    <td>@Model.status.Product.Ram.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Sabit Disk:</td>
                                @if (Model.status.Product.hardDriveId != null)
                                {
                                    <td>@Model.status.Product.HardDrive.name</td>
                                }
                            </tr>

                        </tbody>
                    </table>
                </div>

                {
                    if (Model.user != null)
                    {
                        <div class="border border-rounded col-md-6 rounded">
                            <div class="row bg-primary">
                                <h3 class="pt-3 text-light">Uzman Detayları</h3>
                            </div>
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <td>Ad:</td>
                                        <td>@Model.user.name</td>
                                    </tr>
                                    <tr>
                                        <td>Soyad:</td>
                                        <td>@Model.user.surname</td>
                                    </tr>
                                    <tr>
                                        <td>Yaş:</td>
                                        <td>@Model.user.age</td>
                                    </tr>
                                    <tr>
                                        <td>Cinsiyet:</td>
                                        <td>@Model.user.gender</td>
                                    </tr>
                                    <tr>
                                        <td>Departman:</td>
                                        <td>@Model.user.department</td>
                                    </tr>
                                    <tr>
                                        <td>Lokasyon:</td>
                                        <td>@Model.user.roomNo</td>
                                    </tr>
                                    <tr>
                                        <td>E-Mail:</td>
                                        <td>@Model.user.Email</td>
                                    </tr>

                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="border border-rounded col-md-6 rounded">
                            <div class="row bg-primary">
                                <h3 class="pt-3 text-light">Uzman Detayları</h3>
                            </div>
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <td>Ad:</td>
                                        @*<td>@Model.status.Personel.name</td>*@
                                    </tr>
                                    <tr>
                                        <td>Soyad:</td>
                                        @*<td>@Model.status.Personel.surname</td>*@
                                    </tr>
                                    <tr>
                                        <td>Yaş:</td>
                                        @*<td>@Model.status.Personel.age</td>*@
                                    </tr>
                                    <tr>
                                        <td>Cinsiyet:</td>
                                        @*<td>@Model.status.Personel.Sex.name</td>*@
                                    </tr>
                                    <tr>
                                        <td>Departman:</td>
                                        @*<td>@Model.status.Personel.Department.name</td>*@
                                    </tr>
                                    <tr>
                                        <td>Lokasyon:</td>
                                        @*<td>@Model.status.Personel.roomNo</td>*@
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    }
                }
                <div class="col-md-12 border border-rounded  rounded">
                    <div class="row bg-primary">
                        <h3 class="pt-3 text-light">Durum Detayları</h3>
                    </div>
                    <div class="row">
                        <div class="col-md-2">
                            <button type="button" class="btn" style="border:none" data-bs-toggle="modal" data-bs-target="#exampleModal1">
                                <img src="/productImages/@Model.status.Product.imgName" class="img-fluid border rounded p-2 shadow" alt="...">

                            </button>

                            <!-- Modal -->
                            <div class="modal fade" id="exampleModal1" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                                <div class="modal-dialog modal-dialog-centered">
                                    <div class="modal-content">
                                        <div class="modal-body">
                                            <div class="text-center">
                                                <img src="/productImages/@Model.status.Product.imgName" class="img-fluid border rounded shadow" alt="...">

                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-9">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <td>Açıklama:</td>
                                        <td>@Model.status.definition</td>
                                    </tr>
                                    <tr>
                                        <td>Yerleşke:</td>
                                        @if (Model.status.campusId != null)
                                        {
                                            <td>@Model.status.Campus.name</td>
                                        }
                                    </tr>
                                    <tr>
                                        <td>Son Güncelleştirme Tarihi:</td>
                                        <td>@Model.status.dateOfUpdate</td>
                                    </tr>
                                    <tr>
                                        <td>Kullanıcı Kimliği:</td>
                                        <td>@Model.status.bstId</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>

                    </div>

                    <div class="text-end mt-2 pb-3">
                        @if (Model.status.serviceId == null)
                        {
                            <a class="btn btn-success" asp-controller="editor" asp-action="addservice" asp-route-id="@Model.status.id">Servis Kaydı Oluştur</a>
                        }
                        @if (Model.status.serviceId != null)
                        {
@*                            <a class="btn btn-dark" asp-controller="editor" asp-action="deleteservice" asp-route-id="@Model.status.serviceId">Servis Kaydını Kapat</a>
                            <a class="btn btn-info text-white" asp-controller="editor" asp-action="updateservice" asp-route-id="@Model.status.id">Servis Kaydını Güncelle</a>*@
                            <a class="btn btn-info text-white" asp-controller="editor" asp-action="Detailservice" asp-route-id="@Model.status.id">Servis Kaydı Detayları</a>

                        }
                        <a class="btn btn-primary" asp-controller="editor" asp-action="addproductatpersonel" asp-route-id="@Model.status.id">Personele Ata</a>
                        <a class="btn btn-primary" asp-controller="editor" asp-action="addcommon" asp-route-id="@Model.status.id">Ortağa Ata</a>
                        <a class="btn text-end text-white" style="background-color:#90A4AE" asp-controller="editor" asp-action="updateproduct" asp-route-id="@Model.status.id">Güncelle </a>
                        <a class="btn text-end text-white" style="background-color:#607D8B" asp-controller="editor" asp-action="listofproduct">Geri</a>

                        <!-- Button trigger modal -->
                        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#exampleModal">
                            Sil
                        </button>

                        <!-- Modal -->
                        <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="exampleModalLabel">Uyarı</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body text-start">
                                        Silme işlemi için emin misiniz? Bu işlem geri alınamaz!
                                    </div>
                                    <div class="modal-footer">
                                        <a class="btn btn-danger" asp-controller="editor" asp-action="deleteproduct" asp-route-id="@Model.status.id" asp-route-productId="@Model.status.productId">Evet</a>
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hayır</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
            @if (Model.status.personelId != null && Model.status.commonId == null)
            {
                <div class=" border col-md-4 rounded">

                    <div class="row bg-primary">
                        <h3 class="pt-3 text-light">Ürün Detayları</h3>
                    </div>
                    <table class="table table-borderless">
                        <tbody>
                            <tr>
                                <td>Ad:</td>
                                <td>@Model.status.Product.name</td>
                            </tr>
                            <tr>
                                <td>Kategori:</td>
                                @if (Model.status.Product.categoryId != null)
                                {
                                    <td>@Model.status.Product.Category.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Marka:</td>
                                @if (Model.status.Product.brandId != null)
                                {
                                    <td>@Model.status.Product.Brand.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Model:</td>
                                @if (Model.status.Product.modelId != null)
                                {
                                    <td>@Model.status.Product.Model.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Seri Numarası:</td>
                                <td>@Model.status.Product.serialNo</td>
                            </tr>
                            <tr>
                                <td>Envanter Numarası:</td>
                                <td>@Model.status.Product.inventoryNo</td>
                            </tr>

                            <tr>
                                <td>İşlemci:</td>

                                @if (Model.status.Product.cpuId != null)
                                {
                                    <td>@Model.status.Product.Cpu.name</td>
                                }

                            </tr>
                            <tr>
                                <td>Ekran Kartı:</td>
                                @if (Model.status.Product.gpuId != null)
                                {
                                    <td>@Model.status.Product.Gpu.name</td>
                                }
                            </tr>
                            <tr>

                                <td>Ram:</td>
                                @if (Model.status.Product.ramId != null)
                                {
                                    <td>@Model.status.Product.Ram.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Sabit Disk:</td>
                                @if (Model.status.Product.hardDriveId != null)
                                {
                                    <td>@Model.status.Product.HardDrive.name</td>
                                }
                            </tr>

                        </tbody>
                    </table>
                </div>
                <div class="border border-rounded col-md-4 rounded">
                    <div class="row bg-primary">
                        <h3 class="pt-3 text-light">Personel Detayları</h3>
                    </div>
                    <table class="table table-borderless">
                        <tbody>
                            <tr>
                                <td>Ad:</td>
                                <td>@Model.status.Personel.name</td>
                            </tr>
                            <tr>
                                <td>Soyad:</td>
                                <td>@Model.status.Personel.surname</td>
                            </tr>
                            <tr>
                                <td>Yaş:</td>
                                <td>@Model.status.Personel.age</td>
                            </tr>
                            <tr>
                                <td>Cinsiyet:</td>
                                @if (Model.status.Personel.sexId != null)
                                {
                                    <td>@Model.status.Personel.Sex.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Departman:</td>
                                @if (Model.status.Personel.departmentId != null)
                                {
                                    <td>@Model.status.Personel.Department.name</td>
                                }
                            </tr>
                            <tr>
                                <td>E-Mail:</td>
                                <td>@Model.status.Personel.eMail</td>
                            </tr>
                            <tr>
                                <td>Lokasyon:</td>
                                @if (Model.status.Personel.cLocationId != null)
                                {
                                    <td>@Model.status.Personel.CLocation.name</td>
                                }
                            </tr>
                        </tbody>
                    </table>
                </div>
                <div class="border border-rounded col-md-4 rounded">
                    <div class="row bg-primary">
                        <h3 class="pt-3 text-light">Uzman Detayları</h3>
                    </div>
                    @{
                        if (Model.user != null)
                        {
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <td>Ad:</td>
                                        <td>@Model.user.name</td>
                                    </tr>
                                    <tr>
                                        <td>Soyad:</td>
                                        <td>@Model.user.surname</td>
                                    </tr>
                                    <tr>
                                        <td>Yaş:</td>
                                        <td>@Model.user.age</td>
                                    </tr>
                                    <tr>
                                        <td>Cinsiyet:</td>
                                        <td>@Model.user.gender</td>
                                    </tr>
                                    <tr>
                                        <td>Departman:</td>
                                        <td>@Model.user.department</td>
                                    </tr>
                                    <tr>
                                        <td>Lokasyon:</td>
                                        <td>@Model.user.roomNo</td>
                                    </tr>
                                    <tr>
                                        <td>E-Mail:</td>
                                        <td>@Model.user.Email</td>
                                    </tr>

                                </tbody>
                            </table>
                        }
                        else
                        {
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <td>Ad:</td>
                                        @*<td>@Model.status.Personel.name</td>*@
                                    </tr>
                                    <tr>
                                        <td>Soyad:</td>
                                        @*<td>@Model.status.Personel.surname</td>*@
                                    </tr>
                                    <tr>
                                        <td>Yaş:</td>
                                        @*<td>@Model.status.Personel.age</td>*@
                                    </tr>
                                    <tr>
                                        <td>Cinsiyet:</td>
                                        @*<td>@Model.status.Personel.Sex.name</td>*@
                                    </tr>
                                    <tr>
                                        <td>Departman:</td>
                                        @*<td>@Model.status.Personel.Department.name</td>*@
                                    </tr>
                                    <tr>
                                        <td>Lokasyon:</td>
                                        @*<td>@Model.status.Personel.roomNo</td>*@
                                    </tr>
                                </tbody>
                            </table>
                        }
                    }

                </div>
                <div class="col-md-12 border border-rounded  rounded">
                    <div class="row bg-primary">
                        <h3 class="pt-3 text-light">Durum Detayları</h3>
                    </div>
                    <div class="row">
                        <div class="col-md-2">
                            <button type="button" class="btn" style="border:none" data-bs-toggle="modal" data-bs-target="#exampleModal1">
                                <img src="/productImages/@Model.status.Product.imgName" class="img-fluid border rounded p-2 shadow" alt="...">

                            </button>

                            <!-- Modal -->
                            <div class="modal fade" id="exampleModal1" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                                <div class="modal-dialog modal-dialog-centered">
                                    <div class="modal-content">
                                        <div class="modal-body">
                                            <div class="text-center">
                                                <img src="/productImages/@Model.status.Product.imgName" class="img-fluid border rounded shadow" alt="...">

                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-9">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <td>Açıklama:</td>
                                        <td>@Model.status.definition</td>
                                    </tr>
                                    <tr>
                                        <td>Yerleşke:</td>
                                        @if (Model.status.campusId != null)
                                        {
                                            <td>@Model.status.Campus.name</td>
                                        }
                                    </tr>
                                    <tr>
                                        <td>Son Güncelleştirme Tarihi:</td>
                                        <td>@Model.status.dateOfUpdate</td>
                                    </tr>
                                    <tr>
                                        <td>Kullanıcı Kimliği:</td>
                                        <td>@Model.status.bstId</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div class="text-end mt-2 pb-3">
                        @if (Model.status.serviceId == null)
                        {
                            <a class="btn btn-success" asp-controller="editor" asp-action="addservice" asp-route-id="@Model.status.id">Servis Kaydı Oluştur</a>
                        }
                        @if (Model.status.serviceId != null)
                        {
@*                            <a class="btn btn-dark" asp-controller="editor" asp-action="deleteservice" asp-route-id="@Model.status.serviceId">Servis Kaydını Kapat</a>
                            <a class="btn btn-info" asp-controller="editor" asp-action="updateservice" asp-route-id="@Model.status.id">Servis Kaydını Güncelle</a>*@
                            <a class="btn btn-info" asp-controller="editor" asp-action="Detailservice" asp-route-id="@Model.status.id">Servis Kaydı Detayları</a>

                        }
                        <a class="btn btn-primary" asp-controller="editor" asp-action="movetocommon" asp-route-id="@Model.status.id">Ortağa Ata</a>
                        <a class="btn text-light" style="background-color:#455A64" asp-controller="editor" asp-action="UpdateProductatpersonel" asp-route-id="@Model.status.id">Güncelle </a>
                        <!-- Button trigger modal -->
                        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#exampleModal">
                            Sil
                        </button>

                        <!-- Modal -->
                        <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="exampleModalLabel">Uyarı</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body text-start">
                                        Silme işlemi için emin misiniz? Bu işlem geri alınamaz!
                                    </div>
                                    <div class="modal-footer">
                                        <a class="btn btn-danger" asp-controller="editor" asp-action="deleteproductatpersonel" asp-route-id="@Model.status.id" asp-route-productId="@Model.status.productId">Evet</a>
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hayır</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <a class="btn text-end btn-secondary" asp-controller="editor" asp-action="listofproductatpersonel">Back</a>
                    </div>
                </div>

            }
            @if (Model.status.commonId != null)
            {
                <div class=" border col-md-4 rounded">

                    <div class="row bg-primary">
                        <h3 class="pt-3 text-light">Ürün Detayları</h3>
                    </div>
                    <table class="table table-borderless">
                        <tbody>
                            <tr>
                                <td>Ad:</td>
                                <td>@Model.status.Product.name</td>
                            </tr>
                            <tr>
                                <td>Kategori:</td>
                                @if (Model.status.Product.categoryId != null)
                                {
                                    <td>@Model.status.Product.Category.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Marka:</td>
                                @if (Model.status.Product.brandId != null)
                                {
                                    <td>@Model.status.Product.Brand.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Model:</td>
                                @if (Model.status.Product.modelId != null)
                                {
                                    <td>@Model.status.Product.Model.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Seri Numarası:</td>
                                <td>@Model.status.Product.serialNo</td>
                            </tr>
                            <tr>
                                <td>Envanter Numarası:</td>
                                <td>@Model.status.Product.inventoryNo</td>
                            </tr>

                            <tr>
                                <td>İşlemci:</td>

                                @if (Model.status.Product.cpuId != null)
                                {
                                    <td>@Model.status.Product.Cpu.name</td>
                                }

                            </tr>
                            <tr>
                                <td>Ekran Kartı:</td>
                                @if (Model.status.Product.gpuId != null)
                                {
                                    <td>@Model.status.Product.Gpu.name</td>
                                }
                            </tr>
                            <tr>

                                <td>Ram:</td>
                                @if (Model.status.Product.ramId != null)
                                {
                                    <td>@Model.status.Product.Ram.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Sabit Disk:</td>
                                @if (Model.status.Product.hardDriveId != null)
                                {
                                    <td>@Model.status.Product.HardDrive.name</td>
                                }
                            </tr>

                        </tbody>
                    </table>
                </div>
                <div class="border border-rounded col-md-4 rounded">
                    <div class="row bg-primary">
                        <h3 class="pt-3 text-light">Ortak Detayları</h3>
                    </div>
                    <table class="table table-borderless">
                        <tbody>
                            <tr>
                                <td>Departman:</td>
                                @if (Model.status.Common.departmentId != null)
                                {
                                    <td>@Model.status.Common.department.name</td>
                                }
                            </tr>
                            <tr>
                                <td>Lokasyon:</td>
                                @if (Model.status.Common.cLocationId != null)
                                {
                                    <td>@Model.status.Common.CLocation.name</td>

                                }
                            </tr>

                        </tbody>
                    </table>
                </div>
                <div class="border border-rounded col-md-4 rounded">
                    <div class="row bg-primary">
                        <h3 class="pt-3 text-light">Uzman Detayları</h3>
                    </div>
                    @{
                        if (Model.user != null)
                        {
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <td>Ad:</td>
                                        <td>@Model.user.name</td>
                                    </tr>
                                    <tr>
                                        <td>Soyad:</td>
                                        <td>@Model.user.surname</td>
                                    </tr>
                                    <tr>
                                        <td>Yaş:</td>
                                        <td>@Model.user.age</td>
                                    </tr>
                                    <tr>
                                        <td>Cinsiyet:</td>
                                        <td>@Model.user.gender</td>
                                    </tr>
                                    <tr>
                                        <td>Departman:</td>
                                        <td>@Model.user.department</td>
                                    </tr>
                                    <tr>
                                        <td>Lokasyon:</td>
                                        <td>@Model.user.roomNo</td>
                                    </tr>
                                    <tr>
                                        <td>E-Mail:</td>
                                        <td>@Model.user.Email</td>
                                    </tr>

                                </tbody>
                            </table>
                        }
                        else
                        {
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <td>Ad:</td>
                                        @*<td>@Model.status.Personel.name</td>*@
                                    </tr>
                                    <tr>
                                        <td>Soyad:</td>
                                        @*<td>@Model.status.Personel.surname</td>*@
                                    </tr>
                                    <tr>
                                        <td>Yaş:</td>
                                        @*<td>@Model.status.Personel.age</td>*@
                                    </tr>
                                    <tr>
                                        <td>Cinsiyet:</td>
                                        @*<td>@Model.status.Personel.Sex.name</td>*@
                                    </tr>
                                    <tr>
                                        <td>Departman:</td>
                                        @*<td>@Model.status.Personel.Department.name</td>*@
                                    </tr>
                                    <tr>
                                        <td>Lokasyon:</td>
                                        @*<td>@Model.status.Personel.roomNo</td>*@
                                    </tr>
                                    <tr>
                                        <td>E-Mail:</td>
                                        @*<td>@Model.status.Personel.roomNo</td>*@
                                    </tr>
                                </tbody>
                            </table>
                        }
                    }
                </div>
                <div class="col-md-12 border border-rounded rounded">
                    <div class="row bg-primary">
                        <h3 class="pt-3 text-light">Durum Detayları</h3>
                    </div>
                    <div class="row">
                        <div class="col-md-2">
                            <button type="button" class="btn" style="border:none" data-bs-toggle="modal" data-bs-target="#exampleModal1">
                                <img src="/productImages/@Model.status.Product.imgName" class="img-fluid border rounded p-2 shadow" alt="...">

                            </button>

                            <!-- Modal -->
                            <div class="modal fade" id="exampleModal1" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                                <div class="modal-dialog modal-dialog-centered">
                                    <div class="modal-content">
                                        <div class="modal-body">
                                            <div class="text-center">
                                                <img src="/productImages/@Model.status.Product.imgName" class="img-fluid border rounded shadow" alt="...">

                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-9">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <td>Açıklama:</td>
                                        <td>@Model.status.definition</td>
                                    </tr>
                                    <tr>
                                        <td>Yerleşke:</td>
                                        @if (Model.status.campusId != null)
                                        {
                                            <td>@Model.status.Campus.name</td>
                                        }
                                    </tr>
                                    <tr>
                                        <td>Son Güncelleştirme Tarihi:</td>
                                        <td>@Model.status.dateOfUpdate</td>
                                    </tr>
                                    <tr>
                                        <td>Kullanıcı Kimliği:</td>
                                        <td>@Model.status.bstId</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div class="text-end mt-2 pb-3">
                        @if (Model.status.serviceId == null)
                        {
                            <a class="btn btn-success" asp-controller="editor" asp-action="addservice" asp-route-id="@Model.status.id">Servis Kaydı Oluştur</a>
                        }
                        @if (Model.status.serviceId != null)
                        {
@*                            <a class="btn btn-dark" asp-controller="editor" asp-action="deleteservice" asp-route-id="@Model.status.serviceId">Servis Kaydını Kapat</a>
                            <a class="btn btn-info" asp-controller="editor" asp-action="updateservice" asp-route-id="@Model.status.id">Servis Kaydını Güncelle</a>*@
                            <a class="btn btn-info" asp-controller="editor" asp-action="Detailservice" asp-route-id="@Model.status.id">Servis Kaydı Detayları</a>

                        }
                        <a class="btn btn-primary" asp-controller="editor" asp-action="movetopersonel" asp-route-id="@Model.status.id">Personele Ata</a>
                        <a class="btn text-light" style="background-color:#455A64" asp-controller="editor" asp-action="updatecommon" asp-route-id="@Model.status.id">Güncelle </a>
                        <!-- Button trigger modal -->
                        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#exampleModal">
                            Sil
                        </button>

                        <!-- Modal -->
                        <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="exampleModalLabel">Uyarı</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body text-start">
                                        Silme işlemi için emin misiniz? Bu işlem geri alınamaz!
                                    </div>
                                    <div class="modal-footer">
                                        <a class="btn btn-danger" asp-controller="editor" asp-action="DeleteProductAtCommon" asp-route-id="@Model.status.id" asp-route-commonId="@Model.status.commonId">Evet</a>
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hayır</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <a class="btn text-end btn-secondary" asp-controller="editor" asp-action="listofproductatcommon">Geri</a>
                    </div>
                </div>

            }
        </div>
    </div>


</div>